import chevronImg from "@/assets/rnkx-chevron.svg";

const PageLoader = () => (
  <div className="min-h-screen bg-background flex items-center justify-center">
    <img src={chevronImg} className="w-12 h-12" alt="" />
  </div>
);

export default PageLoader;

