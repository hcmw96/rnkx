export const useAppleHealthSync = () => ({ syncing: false, sync: async () => {} });
export const useWhoopSync = () => ({ syncing: false, sync: async () => {} });
export const useActivitySync = () => ({ syncing: false, sync: async () => {} });
