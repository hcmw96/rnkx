/**
 * RevenueCat entitlement identifier — must match the dashboard ID exactly
 * (display name is "Premium"; identifier is "Pro").
 * Single source of truth for client + check-entitlement.
 */
export const REVENUECAT_ENTITLEMENT_ID = 'Pro';
